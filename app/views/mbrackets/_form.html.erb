<%= form_for(@mbracket) do |f| %>
  <% if @mbracket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mbracket.errors.count, "error") %> prohibited this mbracket from being saved:</h2>

      <ul>
      <% @mbracket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
